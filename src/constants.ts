/** Stable Cordis and Typert service key. */
export const SERVICE_KEY = 'dshSms'

/** Settings namespace owned by this plugin. */
export const SETTINGS_NAME = 'dsh-sms'

/** Credential reference holding the one atomic Google Messages session document. */
export const CREDENTIAL_NAME = 'DSH_SMS_GOOGLE_SESSION'

/** Plugin identifier used in DSH message provenance. */
export const PLUGIN_ID = 'dsh-sms'
